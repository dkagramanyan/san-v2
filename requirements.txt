# torch / torchvision are installed separately from the CUDA wheel index (see README).
# ninja is installed from conda (anaconda::ninja); a pip ninja conflicts with it.
numpy>=1.20
click>=8.0
pillow>=8.3.1
scipy>=1.7.1
requests>=2.26.0
tqdm>=4.62.2
matplotlib>=3.4.2
imageio>=2.9.0
dill>=0.3.4
psutil>=5.8.0
regex>=2022.3.15
pyspng
ftfy>=6.1.1
# timm 1.x (latest). NOTE: checkpoints that embed the projected discriminator's timm
# feature networks (best_model.pkl, network-snapshot-latest.pt) were saved under 0.4.12
# and will NOT unpickle here -- regenerate the progressive stem chain from scratch under
# this timm. Inference-only snapshots (G_ema only) are unaffected.
timm>=1.0.0
h5py>=3.10.0
hydra-core>=1.3
tensorboard>=2.10
# combra: local optional package (in-training generative-quality metrics).
# Relative path is resolved from the dir you run pip from (i.e. san-v2/).
-e ../wc_cv/combra
