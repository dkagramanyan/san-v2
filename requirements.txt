# Core dependencies for StyleGAN-XL training
# Optimized for PyTorch 2.x and CUDA 12.x/13.x

# PyTorch (install separately with CUDA support)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Core packages
numpy>=1.24.0
click>=8.1.0
pillow>=10.0.0
scipy>=1.11.0
requests>=2.31.0
tqdm>=4.66.0

# CUDA extension building
ninja>=1.11.0

# Visualization and I/O
matplotlib>=3.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Serialization
dill>=0.3.7
psutil>=5.9.0

# Text processing
regex>=2023.8.8
ftfy>=6.1.1

# GUI (optional, for visualizer)
imgui>=2.0.0
glfw>=2.6.0
pyopengl>=3.1.7

# PNG support
pyspng>=0.1.1

# Pretrained models (timm - use recent version for better models)
timm>=0.9.0

# Distributed training (optional, for multi-node)
# torch.distributed is built into PyTorch 2.x

# Flash attention (optional, for memory-efficient attention)
# pip install flash-attn --no-build-isolation

# Triton (optional, for torch.compile optimization)
# pip install triton>=2.1.0
